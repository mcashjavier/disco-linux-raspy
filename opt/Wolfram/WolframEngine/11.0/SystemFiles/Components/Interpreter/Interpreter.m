(* This file loaded by Get/Needs["Interpreter`"]. It must load the package files by calling InterpreterLoader.m,
   and also ensure that Interpreter` context is on $ContextPath, which is not done by InterpreterLoader.
*)

BeginPackage["Interpreter`"]
EndPackage[]

Get["InterpreterLoader`"]